query getAllCustomersAndFeatures {
  customers {
      name
      organizationId
      effectiveConfiguration {
        feature {
          key
          type
        }
        value
      }
  }
  features {
    key
    type
    defaultValue
  }
}
