---
title: "Authentication"
description: "You must authenticate your requests with an API token."
---

All requests to Enrolla API endpoints must contain an `Authorization` header with a valid API token.

```bash
'Authorization': 'Bearer <api_token>'
```

## API Tokens

Tokens can be created in the [Enrolla Admin Dashboard](https://app.enrolla.io/apiTokens).
